services:
  mock:
    build: .
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/users"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 30s
